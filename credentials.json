{
"description": "Schema for user credentials stored in Firestore",
"collection_path": "/artifacts/{appId}/public/data/credentials",
"fields": {
"username": "string (unique identifier)",
"password": "string (stored as plain text for this demo)",
"uid": "string (links to the specific Firestore user bucket)"
},
"note": "When a user signs in, the app searches this collection for a matching username/password pair to retrieve the associated UID."
}
